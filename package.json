{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "An app that writes, saves, and deletes notes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zach nylen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql": "^2.16.0"
  }
}
